# 一、登录模块（AuthController）

## ✅ 功能需求

- 获取验证码图片（用于登录前验证）
- 用户登录（验证码验证 + 账号密码验证）
- 获取当前登录用户信息
- 退出登录

## 📋 接口设计

| 功能 | 方法 | 路径 | 请求体 | 返回值 |
|------|------|------|--------|--------|
| 获取验证码图片 | GET | `/api/auth/getVerifyCodeImg` | - | `{ "code": 200, "msg": "验证码图片获取成功", "data": { "verifyCodeId": "...", "verifyCodeImgBase64": "...", "expireTime": "..." } }` |
| 用户登录 | POST | `/api/auth/login` | JSON（用户ID、密码、验证码） | `{ "code": 200, "msg": "登录成功", "data": { "user": { "id": 1 } } }` |
| 获取当前用户 | GET | `/api/auth/currentUser` | -（需登录） | `{ "code": 200, "msg": "ok", "data": { "id": 1, "lastLoginTime": "...", "lastLoginIp": "..." } }` |
| 退出登录 | POST | `/api/auth/logout` | - | `{ "code": 200, "msg": "退出成功", "data": null }` |

## 📦 示例返回数据

### 1. 获取验证码图片
```json
{
  "code": 200,
  "msg": "验证码图片获取成功",
  "data": {
    "verifyCodeId": "d872832e-26b2-4926-b5c5-33e32def3634",
    "verifyCodeImgBase64": "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAHgAAAAo...",
    "expireTime": "2025-11-03T18:11:18.693315100+08:00[Asia/Shanghai]"
  }
}
```

### 2. 登录请求体示例
```json
{
  "userid": 1,
  "password": "your_password",
  "verifyCode": "ABCD",
  "verifyCodeId": "d872832e-26b2-4926-b5c5-33e32def3634",
  "clientInfo": {
    "os": "Windows",
    "browser": "Chrome",
    "loginIp": "127.0.0.1"
  }
}
```

### 3. 登录成功返回
```json
{
  "code": 200,
  "msg": "登录成功",
  "data": {
    "user": {
      "id": 1
    }
  }
}
```

### 4. 获取当前用户返回
```json
{
  "code": 200,
  "msg": "ok",
  "data": {
    "id": 1,
    "lastLoginTime": "2025-11-03T18:15:30",
    "lastLoginIp": "127.0.0.1"
  }
}
```

### 5. 退出登录返回
```json
{
  "code": 200,
  "msg": "退出成功",
  "data": null
}
```

## 错误返回示例

### 验证码错误
```json
{
  "code": 400,
  "msg": "验证码错误或已过期",
  "data": null
}
```

### 账号或密码错误
```json
{
  "code": 400,
  "msg": "账号或密码错误",
  "data": null
}
```

### 未登录或会话过期
```json
{
  "code": 401,
  "msg": "未登录或会话已过期",
  "data": null
}
```

